import { Link } from "@tanstack/react-router";

const StatiGamesLogo = () => {
  return (
    <Link to="/">
      <h1 className="text-2xl font-bold">StatiGames</h1>
    </Link>
  );
};

export default StatiGamesLogo;
